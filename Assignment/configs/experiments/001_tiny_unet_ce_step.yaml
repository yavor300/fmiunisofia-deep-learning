training:
  epochs: 5
  batch_size: 2

scheduler:
  name: step_decay

model:
  architecture: tiny_unet
  encoder_name: null
  encoder_weights: null
  in_channels: 3
  num_classes: 19
  base_channels: 16

loss:
  name: cross_entropy
