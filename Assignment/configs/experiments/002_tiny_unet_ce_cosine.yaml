training:
  epochs: 5
  batch_size: 2

scheduler:
  name: cosine_annealing
  t_max: 5
  eta_min: 0.000001

model:
  architecture: tiny_unet
  encoder_name: null
  encoder_weights: null
  in_channels: 3
  num_classes: 19
  base_channels: 16

loss:
  name: cross_entropy
